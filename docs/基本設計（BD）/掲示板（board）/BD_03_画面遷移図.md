# 掲示板 - 画面遷移図

```mermaid
flowchart TD
    Login([ログイン]) --> BoardList[掲示板一覧
/board]
    BoardList --> PostDetail[投稿詳細
/board/:id]
    PostDetail -->|コメント送信| PostDetail
    PostDetail -->|いいね| PostDetail
    subgraph 管理者
    BoardList --> NewPost[新規投稿フォーム]
    NewPost -->|投稿| BoardList
    PostDetail --> EditPost[投稿編集]
    PostDetail --> DeletePost[投稿削除]
    end
```