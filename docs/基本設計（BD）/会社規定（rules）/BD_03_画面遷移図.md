# 会社規定 - 画面遷移図

```mermaid
flowchart TD
    Login([ログイン]) --> RuleList[規定一覧
/rules]
    RuleList --> RuleDetail[規定詳細閲覧]
    RuleDetail -->|既読確認| RuleDetail
    subgraph 管理者
    RuleList --> AddRule[規定作成]
    AddRule -->|保存| RuleList
    RuleDetail --> EditRule[規定編集]
    EditRule --> RuleDetail
    end
```